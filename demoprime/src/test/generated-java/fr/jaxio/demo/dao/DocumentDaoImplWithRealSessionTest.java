/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/dao/DaoImplWithRealSessionTest.e.vm.java
 */
package fr.jaxio.demo.dao;

import java.util.Set;

import javax.inject.Inject;
import javax.persistence.EntityManager;
import javax.persistence.PersistenceContext;

import org.apache.log4j.Logger;

import static com.google.common.collect.Sets.newHashSet;
import static org.fest.assertions.Assertions.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

import fr.jaxio.demo.domain.Document;
import fr.jaxio.demo.service.DocumentGenerator;
import fr.jaxio.demo.dao.DocumentDao;

/**
 * Integration test on DocumentDaoImpl
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations = { "classpath*:spring/applicationContext-test.xml" })
@Transactional
public class DocumentDaoImplWithRealSessionTest {
    @SuppressWarnings("unused")
    private static final Logger log = Logger.getLogger(DocumentDaoImplWithRealSessionTest.class);

    @PersistenceContext
    private EntityManager entityManager;

    @Inject
    private DocumentDao documentDao;

    @Inject
    private DocumentGenerator documentGenerator;

    @Test
    @Rollback
    public void saveAndGet() {
        Document document = documentGenerator.getDocument();

        // add it to a Set before saving (force equals/hashcode)
        Set<Document> set = newHashSet(document, document);
        assertThat(set).hasSize(1);

        documentDao.save(document);
        entityManager.flush();

        // reload it from cache and check equality
        Document model = new Document();
        model.setId(document.getId());
        assertThat(document).isEqualTo(documentDao.get(model));

        // clear cache to force reload from db
        entityManager.clear();

        // pk are equals...
        assertThat(document.getId()).isEqualTo(documentDao.get(model).getId());

        // but since you do not use a business key, equality is lost.
        assertThat(document).isNotEqualTo(documentDao.get(model));
    }
}