<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 ! Source code generated by Celerio, a Jaxio product
 ! Want to use Celerio within your company? email us at info@jaxio.com
 ! Follow us on twitter: @springfuse
 ! Template pack-jsf2-primefaces-sd:src/main/webapp/WEB-INF/flows/edit/editFlow.e.vm.xml
-->
<flow xmlns="http://www.springframework.org/schema/webflow" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">

	<persistence-context />

	<!-- ===================== -->
	<!-- Flow input parameters -->
	<!-- ===================== -->
	<!-- * role: the entity to edit/create -->
	<!-- * sub: when sub is true, we assume this flow is used as a subflow by a parent flow. -->
	<!-- * readonly: open the passed role in read-only mode -->
	<input name="role" required="true" value="flowScope.role"/>
	<input name="sub" type="boolean" value="flowScope.sub"/>
	<input name="readonly" type="boolean" />

	<decision-state id="startChoice">
		<if test="sub" then="roleSubEdit" else="roleEdit" />
	</decision-state>
	<!-- ==== -->
	<!-- EDIT -->
	<!-- ==== -->
	<view-state id="roleEdit" model="role" parent="#roleEditCommon">
		<on-render>
			<evaluate expression="flowsMenuHandler.updateMenu(flowRequestContext,role.id,roleConverter.print(role))" /> 
		</on-render>

		<!-- event handlers -->
		<transition on="send">
			<evaluate expression="messageUtil.info('status_submitted_ok')" />		
		</transition>

		<transition on="save">
			<evaluate expression="roleRepository.save(flowScope.role)" />
			<evaluate expression="messageUtil.info('status_saved_ok')" />
		</transition>

		<transition on="saveAndClose">
			<evaluate expression="roleRepository.save(flowScope.role)" />
			<evaluate expression="primeFacesUtil.forceClose()"/>
		</transition>

		<transition on="close" validate="false" >
			<evaluate expression="primeFacesUtil.showAskForSaveDialog(persistenceContext, role)"/>
		</transition>

		<transition on="quit" validate="false">
			<set name="conversationScope.endParam" value="'_cascadeQuit'" />
			<!--  TODO -->
			<evaluate expression="primeFacesUtil.showAskForSaveDialog(persistenceContext, role)"/>
		</transition>

		<!-- transitions -->
		<transition on="deleteAndClose" to="deleteAndClose" validate="false">
			<evaluate expression="roleRepository.delete(flowScope.role.id)" />
		</transition>
	</view-state>

	<!-- ======== -->
	<!-- SUB-EDIT -->
	<!-- ======== -->
	<view-state id="roleSubEdit" model="role" parent="#roleEditCommon">
		<!-- events handlers -->
		<transition on="ok">
			<evaluate expression="primeFacesUtil.forceClose()"/>
		</transition>

		<!-- transitions -->
		<transition on="cancel" to="cancelEdit" validate="false" />
	</view-state>

	<!-- =========== -->
	<!-- COMMON-EDIT -->
	<!-- =========== -->
	<view-state id="roleEditCommon" model="role">
	</view-state>

	<!-- ========== -->
	<!-- END STATES -->
	<!-- ========== -->
	<end-state id="endEdit">
		<output name="role" value="role" />
	</end-state>
	<end-state id="cancelEdit" />
	<end-state id="deleteAndClose" />

	<!-- see the SimpleExceptionHandler java code -->
	<end-state id="error" />

	<!-- ================== -->
	<!-- GLOBAL TRANSITIONS -->
	<!-- ================== -->
	<global-transitions>
		<!-- transitions -->
		<transition on="forceClose" to="endEdit" validate="false"/>

		<!-- testing errors -->
		<transition on="ajaxDebugThrowException">
			<evaluate expression="exceptionUtil.throwRuntimeException()" />
		</transition>
		<transition on="debugThrowException" to="startChoice">
			<evaluate expression="exceptionUtil.throwRuntimeException()" />
		</transition>
	</global-transitions>

	<!-- ========== -->
	<!-- EXCEPTIONS -->
	<!-- ========== -->
	<exception-handler bean="simpleExceptionHandler"/>
</flow>