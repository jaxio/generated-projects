/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/RepositoryTest.e.vm.java
 */
package com.jaxio.repository;

import static org.fest.assertions.Assertions.assertThat;
import static org.mockito.Matchers.any;
import static org.mockito.Mockito.times;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.runners.MockitoJUnitRunner;

import com.jaxio.domain.Legacy;
import com.jaxio.repository.LegacyRepository;
import com.jaxio.dao.LegacyDao;
import com.jaxio.dao.support.SearchParameters;

/**
 * Unit test on LegacyRepository
 */
@RunWith(MockitoJUnitRunner.class)
public class LegacyRepositoryTest {

    @Mock
    private LegacyDao legacyDao;

    @InjectMocks
    private LegacyRepository legacyRepository;

    @Test
    public void findUniqueOrNoneReturnsNull() {
        when(legacyDao.findUniqueOrNone(any(Legacy.class), any(SearchParameters.class))).thenReturn(null);

        Legacy result = legacyRepository.findUniqueOrNone(new Legacy());

        assertThat(result).isNull();
        verify(legacyDao, times(1)).findUniqueOrNone(any(Legacy.class), any(SearchParameters.class));
    }

    @Test
    public void findUniqueOrNoneReturnsSingleValue() {
        Legacy unique = new Legacy();

        when(legacyDao.findUniqueOrNone(any(Legacy.class), any(SearchParameters.class))).thenReturn(unique);

        Legacy result = legacyRepository.findUniqueOrNone(new Legacy());

        assertThat(result).isNotNull().isSameAs(unique);
        verify(legacyDao, times(1)).findUniqueOrNone(any(Legacy.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void findUniqueOrNoneThrowsExceptionWhenNonUniqueResults() {
        when(legacyDao.findUniqueOrNone(any(Legacy.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        legacyRepository.findUniqueOrNone(new Legacy());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void findUniqueThrowsExceptionWhenNoResult() {
        when(legacyDao.findUnique(any(Legacy.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        legacyRepository.findUnique(new Legacy());
    }

    @Test
    public void findUnique() {
        Legacy unique = new Legacy();

        when(legacyDao.findUnique(any(Legacy.class), any(SearchParameters.class))).thenReturn(unique);

        Legacy result = legacyRepository.findUnique(new Legacy());

        assertThat(result).isNotNull();
        verify(legacyDao, times(1)).findUnique(any(Legacy.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void findUniqueThrowsExeptionWhenNonUniqueResult() {
        when(legacyDao.findUnique(any(Legacy.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        legacyRepository.findUnique(new Legacy());
    }
}