/*
 * (c) Copyright 2005-2013 JAXIO, http://www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-backend-jpa:src/test/java/service/RepositoryTest.e.vm.java
 */
package com.jaxio.repository;

import static org.fest.assertions.Assertions.assertThat;
import static org.mockito.Matchers.any;
import static org.mockito.Mockito.times;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

import javax.persistence.NonUniqueResultException;
import javax.persistence.NoResultException;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.runners.MockitoJUnitRunner;

import com.jaxio.domain.Document;
import com.jaxio.repository.DocumentRepository;
import com.jaxio.dao.DocumentDao;
import com.jaxio.dao.support.SearchParameters;

/**
 * Unit test on DocumentRepository
 */
@RunWith(MockitoJUnitRunner.class)
public class DocumentRepositoryTest {

    @Mock
    private DocumentDao documentDao;

    @InjectMocks
    private DocumentRepository documentRepository;

    @Test
    public void findUniqueOrNoneReturnsNull() {
        when(documentDao.findUniqueOrNone(any(Document.class), any(SearchParameters.class))).thenReturn(null);

        Document result = documentRepository.findUniqueOrNone(new Document());

        assertThat(result).isNull();
        verify(documentDao, times(1)).findUniqueOrNone(any(Document.class), any(SearchParameters.class));
    }

    @Test
    public void findUniqueOrNoneReturnsSingleValue() {
        Document unique = new Document();

        when(documentDao.findUniqueOrNone(any(Document.class), any(SearchParameters.class))).thenReturn(unique);

        Document result = documentRepository.findUniqueOrNone(new Document());

        assertThat(result).isNotNull().isSameAs(unique);
        verify(documentDao, times(1)).findUniqueOrNone(any(Document.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void findUniqueOrNoneThrowsExceptionWhenNonUniqueResults() {
        when(documentDao.findUniqueOrNone(any(Document.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        documentRepository.findUniqueOrNone(new Document());
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NoResultException.class)
    public void findUniqueThrowsExceptionWhenNoResult() {
        when(documentDao.findUnique(any(Document.class), any(SearchParameters.class))).thenThrow(NoResultException.class);

        documentRepository.findUnique(new Document());
    }

    @Test
    public void findUnique() {
        Document unique = new Document();

        when(documentDao.findUnique(any(Document.class), any(SearchParameters.class))).thenReturn(unique);

        Document result = documentRepository.findUnique(new Document());

        assertThat(result).isNotNull();
        verify(documentDao, times(1)).findUnique(any(Document.class), any(SearchParameters.class));
    }

    @SuppressWarnings("unchecked")
    @Test(expected = NonUniqueResultException.class)
    public void findUniqueThrowsExeptionWhenNonUniqueResult() {
        when(documentDao.findUnique(any(Document.class), any(SearchParameters.class))).thenThrow(NonUniqueResultException.class);

        documentRepository.findUnique(new Document());
    }
}