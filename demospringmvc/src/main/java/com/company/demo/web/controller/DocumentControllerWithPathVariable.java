/*
 * (c) Copyright 2005-2012 JAXIO, www.jaxio.com
 * Source code generated by Celerio, a Jaxio product
 * Want to use Celerio within your company? email us at info@jaxio.com
 * Follow us on twitter: @springfuse
 * Template pack-mvc-3-sd:src/main/java/web/controller/controllerwithPathVariable.e.vm.java
 */
package com.company.demo.web.controller;

import static org.springframework.web.bind.annotation.RequestMethod.DELETE;
import static org.springframework.web.bind.annotation.RequestMethod.GET;
import static org.springframework.web.bind.annotation.RequestMethod.POST;
import static org.springframework.web.bind.annotation.RequestMethod.PUT;

import java.io.IOException;

import javax.servlet.http.HttpServletResponse;
import javax.validation.Valid;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;

import com.company.demo.domain.Document;
import com.company.demo.repository.DocumentRepository;

@Controller
@RequestMapping("/domain/document/")
public class DocumentControllerWithPathVariable {
    @Autowired
    private DocumentRepository documentRepository;

    /**
     * This method is invoked by Spring MVC before the handler methods.
     * <p>
     * The path variable is converted by SpringMVC to a Document via the {@link DocumentFormatter}.
     * Before being passed as an argument to the handler, SpringMVC binds the attributes on the resulting model,
     * then each handler method may receive the entity, potentially modified, as an argument.
     */
    @ModelAttribute
    public Document getDocument(@PathVariable("pk") Document document) {
        return document;
    }

    /**
     * Serves the show view for the entity.
     */
    @RequestMapping("show/{pk}")
    public String show(@ModelAttribute Document document) {
        return "domain/document/show";
    }

    /**
     * Support for file download.
     */
    @RequestMapping( { "download/documentBinary/{pk}", "download/documentBinary/{pk}/*" })
    public void documentBinaryView(@ModelAttribute Document document, HttpServletResponse servletResponse)
            throws IOException {
        servletResponse.setContentLength(document.getDocumentSize());
        servletResponse.setContentType(document.getDocumentContentType());
        servletResponse.getOutputStream().write(document.getDocumentBinary());
    }

    /**
     * Serves the update form view.
     */
    @RequestMapping(value = "update/{pk}", method = GET)
    public String update() {
        return "domain/document/update";
    }

    /**
     * Performs the update action and redirect to the show view.
     */
    @RequestMapping(value = "update/{pk}", method = { PUT, POST })
    public String update(@Valid @ModelAttribute Document document, BindingResult bindingResult) {
        if (bindingResult.hasErrors()) {
            return update();
        }

        documentRepository.save(document);
        return "redirect:/domain/document/show/" + document.getId();
    }

    /**
     * Serves the delete form asking the user if the entity should be really deleted.
     */
    @RequestMapping(value = "delete/{pk}", method = GET)
    public String delete() {
        return "domain/document/delete";
    }

    /**
     * Performs the delete action and redirect to the search view.
     */
    @RequestMapping(value = "delete/{pk}", method = { PUT, POST, DELETE })
    public String delete(@ModelAttribute Document document) {
        documentRepository.delete(document);
        return "redirect:/domain/document/search";
    }

}